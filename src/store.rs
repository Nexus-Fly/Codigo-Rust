/// In-memory key-value store. Placeholder for future state management.
#[derive(Debug, Default)]
pub struct Store {
    // Will hold application state as the project grows.
}

impl Store {
    pub fn new() -> Self {
        Self::default()
    }
}
