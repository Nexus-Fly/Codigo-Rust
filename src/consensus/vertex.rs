/// Thin wrapper around the Tashi Vertex engine.
/// Placeholder – real integration lives in `main.rs` for now.
#[derive(Debug, Default)]
pub struct VertexNode;

impl VertexNode {
    pub fn new() -> Self {
        Self
    }
}
